# Application Configuration
PORT=4000
NODE_ENV=development
API_PREFIX=/api/v1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Database Connection (PostgreSQL)
DATABASE_URL=postgresql://sector_admin:sector_secure_password_2026@localhost:5432/sector_db?schema=public

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Authentication & Security
JWT_SECRET=super_secret_enterprise_jwt_signing_key_sector_2026_change_in_prod
JWT_EXPIRATION=1d
COOKIE_SECRET=super_secret_cookie_parser_key_sector_2026

# Sri Lanka SMS Gateway Configuration (Notify.lk / Provider)
SMS_PROVIDER=NOTIFY_LK
NOTIFY_LK_USER_ID=
NOTIFY_LK_API_KEY=
NOTIFY_LK_SENDER_ID=SECTOR_SL

# Email Configuration (SMTP / Nodemailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=no-reply@sector.lk
